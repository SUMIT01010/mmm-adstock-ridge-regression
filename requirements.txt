# Regression — Adstock-Ridge Marketing Mix Model
# Pinned to Python 3.11 (see ../skills/uv-python311-ml-env-on-mac.md).
# Create env:  uv venv --python 3.11 && uv pip install -r requirements.txt
#
# NOTE: no torch, no xgboost/lightgbm anywhere in this project — pure numpy/pandas/
# scikit-learn/scipy. Sidesteps the Mac libomp import-order issue entirely.

# --- core numerics ---
numpy>=1.26,<3
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
statsmodels>=0.14      # VIF, Ljung-Box, Chow-style structural break F-test, QQ plot

# --- data access ---
requests>=2.31
pyarrow>=15             # parquet I/O

# --- serving ---
streamlit>=1.33
joblib>=1.3

# --- plotting / reporting / PDF book ---
matplotlib>=3.8
